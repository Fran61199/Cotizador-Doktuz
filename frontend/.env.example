# URL base de la API (backend)
# Local: http://localhost:8000
# Producción: https://api-cotizador.onrender.com (o tu subdominio)
NEXT_PUBLIC_API_URL=http://localhost:8000

# NextAuth + Google OAuth
# Local: http://localhost:3000
# Producción: https://cotizador.tudominio.com (o tu URL de Vercel)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=genera-uno-con-openssl-rand-base64-32
GOOGLE_CLIENT_ID=tu-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=tu-client-secret

# Secret compartido con el backend para validar peticiones del proxy (obligatorio si usas proxy).
# Debe coincidir con BACKEND_API_SECRET del backend. Genera uno: openssl rand -base64 32
BACKEND_API_SECRET=genera-uno-con-openssl-rand-base64-32

# Usuario por defecto (seed_db): admin@doktuz.com / admin123

# Opcional: SKIP_AUTH=true para desarrollo sin login (no usar en producción)
